# v0.3.0

> 本版核心：**预测管线故障根治**（连续 4 天 38 题跳过的根因）+ **Ponytail 全量代码精简**（净减 ~750 行，零新依赖、零行为变更）。

**[English →](v0.3.0-en.md)**

---

## 预测管线修复

- **预测跳过根治**：DeepSeek Responses API 开启 `json_format` 时要求 `input` 含 "json" 字样，否则恒 400——`websearch_predictor` 与 `llm_resolver` 的输入模板补齐 JSON 输出示例，38 题跳过归零
- **异步入口默认参数补齐**：`aresponses_create` 的 `max_output_tokens/temperature/json_format/timeout` 补默认值，调用方不再因缺参 TypeError 全体失败
- **存量题回补**：17 题从未预测 + 7 天待更新题全部补测（`predicted=17, skipped=0`）

## 代码精简（Ponytail 四路并行）

- `ops/health.py` 275→99：`_c` 构造 helper、`_union_predict` 内联、探测检查循环化、scoreboard 表驱动
- `scripts/evolve.py` 340→171：删 arm B 实验臂（候选恒 None）、`_build_base_rates`（5 key 只命中 1）、`difficulty_tiers` 统计、周报占位 40L→10L
- `scripts/daily.py` 250→169：删重复 import、`_within_update_window` 内联、`_predict_safely` 死参
- `data/storage.py` 1019→936：删 `canonical_model_name` 6 符号（单模型永远走不到 ensemble）、`source_*`/`ops_*` 改 SQL `json_extract` 消除 N+1
- `selection/families.py` 229→202：8 个硬编码题族 spec 抽 `FAMILIES` 常量表
- `websearch_predictor.py` 342→234：文本/引用提取合并去重、心跳内联、删 `_log_skip` 第三拷贝
- `pipeline.py` 152→96：删单模型权重逻辑（恒等权）、`extremize` 参数内联
- `llm/client.py` 232→222：chat/responses 双重试循环抽 `_post_with_retry`、`_has_answer` 提到模块级、token 上限统一 65536
- `web/server.py` 281→248：告警横幅 inline style→CSS 类、`deque`/`Query`/`with_suffix`/`HTTPException` 替手写
- `ops/probes.py` 149→141：`csv.DictReader` 替手切 CSV

## 稳定性

- `crawl_social.py` mtime 比较加 1s 容差：修复 Windows 文件时间戳精度导致的测试 flaky（真实抓取 >30s 不受影响）
- 告警 ack 批量处理：历史 10 条未确认告警清账，dashboard 横幅退出

## 验证

- 全量 pytest 绿；真实 LLM 采样 + 端到端预测（`#107 → 1.67%，29 引用`）实测通过
- dashboard 全端点（`/api/questions` `/api/system` `/api/ops/health` 探针三项）正常
- 定时任务三件套就绪（daily 09:00 / evolve 09:05 / resolve 16:30）
